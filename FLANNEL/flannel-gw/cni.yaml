apiVersion: apps/v1
kind: DaemonSet
metadata:
  labels:
    app: flannel-gw
  name: flannel-gw
spec:
  selector:
    matchLabels:
      app: flannel-gw
  template:
    metadata:
      labels:
        app: flannel-gw
    spec:
      containers:
      - image: burlyluo/nettool
        name: nettoolbox
        securityContext:
          privileged: true
---
apiVersion: v1
kind: Service
metadata:
  name: serversvc
spec:
  type: NodePort
  selector:
    app: flannel-gw
  ports:
  - name: cni
    port: 8080
    targetPort: 80
    nodePort: 32000
